class Solution:
    def findErrorNums(self, nums: List[int]) -> List[int]:
        n=len(nums)
       
        out=[]
        freq={}
        for j in range(1,n+1):
            freq[j]=0
            
        for j in nums:
            if j in freq:
                freq[j]+=1
        
        for key in freq:
            if freq[key]==2:
                out.append(key)
                break
        
        for key in freq:
            if freq[key]==0:
                out.append(key)
                break
        
            
        return out    
        
